{
  "name": "rasad-news",
  "version": "3.0.0",
  "private": true,
  "description": "رصد | اینستاگرامِ اخبار و بازار ایران",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "collect": "node scripts/collect.mjs",
    "serve": "node scripts/serve.mjs",
    "check": "npm test && node scripts/lint-html.mjs",
    "bundle": "node scripts/collect.mjs --bundle-only",
    "demo": "node scripts/demo-data.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
