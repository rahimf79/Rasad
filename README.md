<div align="center">

<img src="assets/img/logo.svg" alt="رصد" width="340">

**اینستاگرامِ اخبار و بازار ایران**

صفحهٔ اختصاصی هر خبرگزاری · استوری ۲۴ ساعته · ریلز · جست‌وجو · قیمت لحظه‌ای از ثروتمندی با نمودار و کامنت

[پیش‌نمایش زنده](https://rahimf79.github.io/Rasad/) · [راهنمای فعال‌سازی](#-فعال‌سازی-روی-github-pages)

</div>

---

## ✨ نسخهٔ ۳ — ظاهر عین اینستاگرام + دادهٔ آماده از GitHub Actions

دو تغییر اصلی این نسخه:

### ۱) رابط کاربری دقیقاً مثل اینستاگرام

چیدمان، اندازه‌ها، آیکون‌ها و رنگ‌ها از اپ و وب رسمی اینستاگرام برداشته شده‌اند (نه «الهام‌گرفته»):

| بخش | اینستاگرام | رصد |
|---|---|---|
| هدر موبایل | لوگوتایپ اسکریپت + فلش (منوی Following/Favorites) · ♡ اعلان‌ها · ✈ دایرکت | `Rasad` با فونت Grand Hotel + فلش (منوی «همه / دنبال‌شده‌ها») · ♡ اعلان‌ها · ✈ گفتگوها |
| ناوبری پایین | ۵ آیکون بدون برچسب: خانه، جست‌وجو، ساخت، ریلز، آواتار | ۵ آیکون: خانه، جست‌وجو، **بازار** (وسط)، ریلز، آواتار — آیکون فعال «پرشده» می‌شود |
| دسکتاپ | نوار کناری با لوگوتایپ و آیکون+برچسب | همان نوار کناری (۷۲px → ۲۴۴px در عرض ≥۱۲۶۴) |
| کارت پست | آواتار ۳۲px با حلقه · نام + تیک آبی + «•» زمان · «دنبال کردن» آبی · ⋯ · رسانهٔ مربعی · ♡ 💬 ✈ … 🔖 · «۱۲ پسند» · کپشن با «بیشتر» · «مشاهدهٔ همهٔ N نظر» · زمان کوچک · «افزودن نظر…» | همان ترتیب و همان اندازه‌ها (`assets/js/render.js → renderPost`) — همهٔ آیکون‌ها SVG خطی ۲۴px، دابل‌کلیک = لایک با انیمیشن قلب |
| استوری | حلقهٔ گرادیانی ۶۶px، نام زیر آن، ویوئر تمام‌صفحه با نوار پیشرفت | همان؛ استوری‌ها از خبرهای ۲۴ ساعت اخیر هر خبرگزاری ساخته می‌شوند |
| پروفایل | آواتار ۸۶px + سه آمار · نام · بیو · دکمهٔ آبی/خاکستری Follow · تب‌های آیکونی · گرید ۳تایی | همان برای هر خبرگزاری (`#/u/irna`) و برای کاربر (`#/me`) |
| منوها | شیت پایین‌صفحه با دستگیره | همان (`renderSheet`) برای ⋯ پست، منوی «بیشتر» و پروفایل |
| رنگ‌ها | سیاه خالص/سفید، خط `#262626`/`#dbdbdb`، آبی `#0095f6`، قرمز `#ff3040` | همان توکن‌ها در `assets/css/app.css` (تیره/روشن/سیستم) |

آیکون‌ها در `assets/js/icons.js` (خطی + پرشده) و لوگوتایپ فقط با فونت Grand Hotel — مثل اینستاگرام که لوگوتایپِ هدرش تک‌رنگ است و گرادیان فقط روی آیکون اپ و حلقهٔ استوری می‌نشیند.

### ۲) مرورگر هیچ خبری جمع نمی‌کند؛ فقط دادهٔ آماده را نشان می‌دهد

```
GitHub Actions (هر ۳۰ دقیقه)                       مرورگر کاربر
────────────────────────────                       ────────────────
scripts/collect.mjs                                index.html
  ├─ RSS خبرگزاری‌ها ──► data/news/YYYY-MM-DD.json   └─ fetch data/latest.json  (یک درخواست)
  ├─ ثروتمندی ────────► data/prices/<key>.json           ├─ فید، استوری، بازار، ریلز … فوراً رندر
  ├─ باما ────────────► data/cars.json                   ├─ کش آفلاین در IndexedDB
  └─ بستهٔ آماده ─────► data/latest.json                 └─ هر ۵ دقیقه فقط همین فایل را دوباره
git commit + push  ──► actions/deploy-pages                می‌خواند → پیل «N پست جدید»
```

- `data/latest.json` («بستهٔ آماده») همهٔ چیزی است که صفحهٔ اول لازم دارد: تا ۴۰۰ پست تازه، آخرین قیمت همهٔ دارایی‌ها + اسپارک‌لاین، قیمت خودرو، وضعیت منابع، زمان تولید و زمان اجرای بعدی.
- هیچ درخواستی به خبرگزاری‌ها/ثروتمندی/باما از سمت کاربر ارسال نمی‌شود (تست «بستهٔ آماده: صفحه بدون هیچ جمع‌آوری‌ای…» این را تضمین می‌کند). «دریافت زنده» فقط یک دکمهٔ آزمایشی در تنظیمات است.
- **نکتهٔ مهم CI:** کامیتی که با `GITHUB_TOKEN` پوش می‌شود ورک‌فلوی `deploy.yml` را فعال نمی‌کند؛ به همین دلیل `collect.yml` بعد از کامیت، خودش Pages را دوباره منتشر می‌کند (وگرنه دادهٔ تازه هرگز به سایت نمی‌رسید). هر دو ورک‌فلو از یک `concurrency: pages` استفاده می‌کنند.
- سری‌های قیمت با `compactSeries` فشرده می‌شوند (۲ روز اخیر کامل، تا ۳۰ روز ساعتی، تا ۱ سال ۶ ساعته، قدیمی‌تر روزانه) تا مخزن با گذشت زمان منفجر نشود.

---

## 🧭 ساختار

```
index.html                  پوستهٔ برنامه (هدر، main، ناوبری، overlay/sheet/story)
assets/css/app.css          سیستم طراحی اینستاگرام (تیره/روشن، موبایل → نوار کناری دسکتاپ)
assets/img/{logo,icon}.svg  لوگوتایپ و آیکون
assets/js/
  app.js                    هسته: بوت از بستهٔ آماده، روتینگ، رندر، رویدادها
  icons.js                  آیکون‌های SVG اینستاگرام (خطی + پرشده)
  render.js                 همهٔ قالب‌ها (پست، استوری، پروفایل، بازار، شیت، ناوبری، هدر…)
  router.js                 روتر هش‌بنیاد (#/, #/u/, #/p/, #/price/, #/activity, #/inbox, #/archive/:day …)
  db.js                     لایهٔ داده: IndexedDB + حافظه + آینهٔ گیت‌هاب (putPostsBulk برای کش بسته)
  posts.js                  پست‌ها، تشخیص موضوع، منطق ۲۴ ساعتهٔ استوری
  prices.js                 نرمال‌سازی قیمت، آمار بازه، نمودار SVG
  search.js                 جست‌وجوی خبرگزاری/پست/قیمت/هشتگ
  collect.js                loadLatestBundle (مسیر اصلی) + جمع‌آوری زنده (فقط دستی)
  config.js                 خبرگزاری‌ها، موضوعات، کدهای ثروتمندی، COLLECT_INTERVAL_MIN
  lib/bundle.js             ساخت/اعتبارسنجی بستهٔ آماده + compactSeries (مشترک Node و مرورگر)
  lib/jalali.js             تبدیل تاریخ جلالی ⇄ میلادی
  lib/parsers.js            پارسرهای مشترک (RSS، ثروتمندی، باما، TGJU)
  lib/util.js               ابزارها + SHA-256 + timeShort/compactNum
scripts/
  collect.mjs               جمع‌آوری سمت سرور (GitHub Actions) → data/ + data/latest.json
  lib/bundle-disk.mjs       ساخت latest.json از روی پوشهٔ data
  seed.mjs                  دادهٔ اولیه از مقادیر واقعی ثروتمندی (+ بسته)
  demo-data.mjs             دادهٔ نمایشی برای پیش‌نمایش آفلاین (کامیت نشود)
  serve.mjs                 سرور استاتیک محلی
  lint-html.mjs             بررسی سازگاری importها/شناسه‌ها/مسیرها/ورک‌فلوها
data/                       «پایگاه داده» — latest.json + آرشیو اخبار و سری زمانی قیمت‌ها
test/                       ۱۰۳ آزمون (node:test + jsdom)
.github/workflows/          deploy.yml (کد) و collect.yml (داده + انتشار Pages)
```

---

## 💹 منابع قیمت

### ثروتمندی (`servatmandi.com`) — منبع اصلی و الزامی

قیمت‌ها مستقیماً از صفحهٔ `https://servatmandi.com/Entity/Summary/{code}` خوانده می‌شوند
(آخرین قیمت، اولین، بیشترین، کمترین، قیمت روز کاری قبلی و **زمان دقیق جلالی تا ثانیه**).
مقادیر ریالی به تومان تبدیل می‌شوند.

| گروه | نمونه دارایی‌ها (کد Entity) |
|---|---|
| ارز | دلار آمریکا `100000000001`، یورو `100000000002`، پوند `100000000008`، درهم `100000000005`، لیر `100000000006`، یوان `100000000007`، فرانک `100000000012`، ۱۰۰ ین `100000000016`، دلار کانادا/استرالیا، روبل، دینار عراق/کویت، ریال عربستان/قطر، افغانی، منات، دلار حواله دولتی `200000000001`، شاخص دلار ثروتمندی `800000` |
| طلا و سکه | طلای ۱۸ عیار `5000000001102`، مظنه طلا `5000000001000`، سکه امامی `5000000001202`، نقره ۹۹۹ `50000000001301`، انس طلا `10000000001901`، انس نقره `10000000001903` |
| نفت، گاز و انرژی | برنت `10000000002001`، WTI `10000000002000`، گاز طبیعی `10000000002002`، TTF هلند `10000000002006`، NBP انگلستان `10000000002007`، گاز آلمان `10000000002014`، LNG `10000000002016`، بنزین `10000000002003`، نفت کوره `10000000002004`، گازوئیل `10000000002009`، پروپان `10000000002010`، زغال سنگ `10000000002005`، زغال کک `10000000002015`، اورانیوم `10000000002011`، اتانول، متانول، نفت اورال |
| ارز دیجیتال | تتر `5000000000000`، بیت‌کوین `920000`، اتریوم `920001`، پکس گلد `910001`، تتر گلد `910000` |

علاوه بر این، `collect.mjs` در هر اجرا دیدبان‌های `Entities/1..6` را پیمایش می‌کند و دارایی‌های جدید را
در `data/entities.json` کشف و ثبت می‌کند (تا با افزودن نماد جدید در ثروتمندی، لازم به تغییر کد نباشد).

### باما (`bama.ir`) — قیمت خودرو
صفحهٔ قیمت روز و صفحهٔ برندها پارس می‌شوند؛ نتیجه در `data/cars.json` و در تب **بازار** با گروه «خودرو» نمایش داده می‌شود.
اگر ساختار صفحهٔ باما تغییر کند و پارسر چیزی پیدا نکند، برنامه بدون خطا ادامه می‌دهد و فقط آن بخش خالی می‌ماند.

---

## 🗄️ ذخیره‌سازی: GitHub Pages دیتابیس ندارد، پس سه لایه داریم

GitHub Pages فقط فایل استاتیک سرو می‌کند. برای اینکه داده‌ها **ریست نشوند** و **با مرور زمان حذف شوند**:

1. **IndexedDB مرورگر** — حساب‌ها، دنبال‌شونده‌ها، لایک، ذخیره، کامنت‌ها و آرشیو محلی. با رفرش پاک نمی‌شود.
2. **مخزن گیت به‌عنوان دیتابیس** — `collect.yml` هر ۳۰ دقیقه اجرا می‌شود، اخبار و قیمت‌ها را جمع می‌کند،
   `data/*.json` و بستهٔ آمادهٔ `data/latest.json` را کامیت می‌کند و Pages را دوباره منتشر می‌کند.
   این داده بین همهٔ کاربران مشترک است و مرورگر فقط آن را می‌خواند.
3. **همگام‌سازی اختیاری با GitHub Contents API** — در `#/settings` می‌توانید یک توکن (fine-grained PAT با دسترسی
   `Contents: Read & Write`) بدهید تا کاربران، دنبال‌شونده‌ها و کامنت‌ها مستقیم روی مخزن (پوشهٔ `user-data/`) نوشته شوند.
   دکمه‌های «آزمون اتصال» و «دریافت از گیت‌هاب» هم در همان بخش است.

### سیاست نگهداری (حذف تدریجی)
```js
RETENTION = { newsDays: 120, priceDays: 1095, commentsDays: 3650 }
```
- اخبار قدیمی‌تر از ۱۲۰ روز از آرشیو حذف می‌شوند (`pruneNews`).
- نقاط قیمتی قدیمی‌تر از ۳ سال حذف می‌شوند (`prunePrices`).
- پست‌ها در برنامه تاریخ انقضا ندارند؛ فقط استوری‌ها ۲۴ ساعته‌اند.
- هر دو مقدار در تنظیمات قابل تغییر است.

---

## 🚀 فعال‌سازی روی GitHub Pages

1. در ریپو: **Settings → Pages → Build and deployment → Source** را روی **GitHub Actions** بگذارید.
2. کد را به شاخهٔ پیش‌فرض (`main`) مرج کنید؛ `deploy.yml` تست‌ها را اجرا می‌کند، اگر بستهٔ آماده هنوز پست ندارد
   یک بار داده جمع می‌کند (best effort) و سایت را منتشر می‌کند — پس همان اولین انتشار هم خالی نیست.
3. `collect.yml` پس از مرج روی `main` هر ۳۰ دقیقه (دقیقهٔ ۷ و ۳۷) اجرا می‌شود: جمع‌آوری → کامیت `data/` → انتشار Pages.
   > GitHub طبق سیاست خود `schedule` را فقط روی شاخهٔ پیش‌فرض اجرا می‌کند. هر وقت خواستید از
   > **Actions → Collect news and prices → Run workflow** دستی هم اجرا بگیرید.
   > فاصلهٔ اجرا: `cron` در `collect.yml` و `APP.COLLECT_INTERVAL_MIN` در `config.js` (برای نمایش «هر N دقیقه» در رابط).
4. اگر می‌خواهید روی دامنهٔ اختصاصی منتشر شود، فایل `CNAME` را در ریشه اضافه کنید (ورک‌فلو خودش آن را به خروجی اضافه می‌کند).

### اجرای محلی
> نیازمند **Node ۲۲.۱۹ یا بالاتر** (وابستگی jsdom یعنی undici به `worker_threads.markAsUncloneable` نیاز دارد).
> روی Node ۲۰ تست یکپارچهٔ jsdom با خطای `markAsUncloneable is not a function` می‌افتد.

```bash
npm install
npm run demo      # دادهٔ نمایشی → data/latest.json (برای دیدن رابط بدون اینترنت؛ کامیت نکنید)
npm run serve     # http://localhost:4173
npm test          # ۱۰۳ آزمون
npm run check     # تست + lint
node scripts/collect.mjs --dry   # جمع‌آوری واقعی بدون نوشتن روی دیسک
npm run bundle    # بازسازی data/latest.json از روی آرشیو data/ (برگرداندن بعد از demo)
```

---

## 🧪 چه چیزی تست شده است؟

`npm test` → **۱۰۳ آزمون** با `node:test`:

- **بستهٔ آماده**: ساخت بسته (سقف پست، بدون تکرار، همهٔ دارایی‌ها با اسپارک‌لاین، دارایی بدون اسنپ‌شات از آرشیو)، `compactSeries`، اعتبارسنجی در مرورگر، و نوشتن `latest.json` از روی پوشهٔ `data`.
- **بدون جمع‌آوری در مرورگر**: بوت با یک بستهٔ ساختگی → تنها درخواست شبکه `data/latest.json` است و هیچ تماسی با rss2json/ثروتمندی/باما نمی‌شود؛ بررسی بستهٔ جدید بدون رفرش اعمال می‌شود.
- **رابط اینستاگرامی**: ترتیب بخش‌های کارت پست، آیکون‌های SVG خطی/پرشده، ۵ آیتم ناوبری موبایل، هدر خانه (لوگوتایپ + فلش + ♡ + ✈)، دکمهٔ برگشت صفحات فرعی، پروفایل با سه آمار و تب‌های آیکونی، شیت و منوی لوگوتایپ.

- **تاریخ جلالی**: رفت‌وبرگشت ۱۲۰۰ روزه، و `۱۴۰۵/۰۶/۳۰ = ۲۰۲۶/۰۹/۲۱` (همان تاریخی که روی صفحهٔ ثروتمندی دیده شد).
- **پارسرها**: RSS/Atom با CDATA، صفحهٔ خلاصهٔ ثروتمندی در دو قالب HTML و markdown، کشف دارایی‌ها، قیمت خودروی باما، تاریخچهٔ TGJU.
- **دیتابیس**: ثبت‌نام/ورود/خروج، عدم نشت هش رمز، **ممنوعیت کامنت بدون ورود**، حذف کامنت فقط توسط نویسنده، دنبال کردن، آرشیو بدون تکراری، پاک‌سازی بر اساس سیاست نگهداری، پشتیبان‌گیری.
- **استوری**: مرز دقیق ۲۴ ساعت، گروه‌بندی بر اساس خبرگزاری، دیده‌شده/دیده‌نشده.
- **قیمت**: تبدیل ریال→تومان، درصد تغییر، آمار بازه، «قیمت در فلان زمان»، تولید SVG نمودار.
- **یکپارچه با jsdom**: همان `index.html` و همان ماژول‌ها بارگذاری می‌شوند؛ مسیر واقعی
  `fetch → parseRss → normalizeItem → Store → render → DOM` بررسی می‌شود،
  شامل باز شدن صفحهٔ خبرگزاری، رسم نمودار قیمت، قفل بودن کامنت برای مهمان، ثبت کامنت پس از ورود،
  و باز شدن مودال ورود هنگام زدن «دنبال کردن».

> ⚠️ محدودیت راستی‌آزمایی: در محیط توسعهٔ این تغییرات، خروجی شبکه به‌جز `api.github.com` بسته بود،
> بنابراین **خودِ درخواست‌های شبکه به ثروتمندی/باما/خبرگزاری‌ها به‌صورت زنده اجرا نشدند**.
> آنچه اجرا شد: اجرای کامل `scripts/collect.mjs` (که همهٔ مسیرهای خطا را بدون crash پیمود)،
> پارسرها روی نمونه‌های واقعیِ صفحه‌ها، و کل منطق برنامه در jsdom با پاسخ‌های شبیه‌سازی‌شده.
> اولین اجرای `collect.yml` روی GitHub Actions راستی‌آزمایی نهایی زنده است.

---

## ⚠️ سلب مسئولیت

> این پروژه صرفاً یک ابزار اطلاع‌رسانی است و هیچ‌گونه توصیهٔ مالی، سرمایه‌گذاری یا معاملاتی ارائه نمی‌دهد.

1. قیمت‌ها از منابع عمومی (ثروتمندی، باما) خوانده می‌شوند و ممکن است با قیمت واقعی بازار تفاوت داشته باشند.
2. محتوای خبرها متعلق به خبرگزاری‌های منبع است؛ این پروژه مسئولیتی در قبال صحت آن ندارد.
3. استفادهٔ غیرمجاز، سوءاستفاده از APIها یا فعالیت مغایر قوانین ممنوع است.
4. این نرم‌افزار «آن‌گونه که هست» و بدون هیچ ضمانتی ارائه می‌شود.
5. مسئولیت هر تصمیم مالی بر عهدهٔ کاربر است.
6. اگر همگام‌سازی گیت‌هاب را فعال می‌کنید، توکن را با دسترسی محدود (فقط همان مخزن) بسازید؛ توکن در `localStorage` همان مرورگر ذخیره می‌شود.

---

## 📄 مجوز

MIT — استفادهٔ شخصی و آموزشی آزاد است.

```
Copyright (c) 2026 rahimf79
```

<div align="center">

**ساخته‌شده با ☕ و کمی وسواس روی جزئیات**

⭐ اگه خوشت اومد، ستاره بده!

</div>
